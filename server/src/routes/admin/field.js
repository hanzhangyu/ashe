// const adminController = require('../controllers/admin');

const apiRoutes = [];

const viewRoutes = [
    {
        url: '/app/admin/field',
    },
];

module.exports = [...apiRoutes, ...viewRoutes];
